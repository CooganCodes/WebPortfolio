import React from 'react';
import './Header.scss';

const Header = () => {
  return (
    <header>
      <div className="logo">YourLogo</div>
    </header>
  );
};

export default Header;
